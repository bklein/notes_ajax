<article <%= 'class="complete"' if note.complete %> id="article_<%= note.id %>">
  <p>
    <%= note.content %>
    <span><a id="task_edit_<%= note.id %>" href="/<%= note.id %>">[edit]</a> | <a id="task_del_<%= note.id %>" href="#">[delete]</a></span>
  </p>
  <p class="links">
    <a href="/<%= note.id %>/complete">&#8632;</a>
  </p>
  <p class="meta">Created: <%= note.created_at %></p>
</article>

